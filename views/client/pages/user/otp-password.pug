doctype html

html
    head
        title #{pageTitle}
        link(href="/css/style.css" rel="stylesheet")
        
    body
        - if (messages.success)
            div(
                class="alert alert-success alert-message"
                alert-message
            ) #{messages.success}
        - if (messages.error)
            div(
                class="alert alert-danger alert-message"
                alert-message
            ) #{messages.error}

        include ../../partials/forgot-header.pug
        
        div(class="forgot__main")
            form(class="forgot__content" action="/user/password/otp" method="POST")
                div(class="forgot-title") Nhập mã bảo mật
                div(class="forgot-form")
                    div(class="forgot-desc") Vui lòng kiểm tra điện thoại để xem tin nhắn văn bản có mã. Mã của bạn có 6 ký tự.
                    div(class="otp-form")
                        input(name="otp" placeholder="Nhập mã" class="otp-input")
                        input(name="email" value=email class="otp-hidden")
                        div(class="otp-info")
                            div(class="otp-desc") Chúng tôi đã gửi mã cho bạn đến:
                            div(class="otp-phone") #{email}
                div(class="forgot-buttons")
                    a(class="forgot-cancel" href="/user/login") Hủy
                    button(class="forgot-search" type="submit") Tiếp tục

        script(src="https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.slim.min.js")
        script(src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js")
        script(src="/js/script.js") 

